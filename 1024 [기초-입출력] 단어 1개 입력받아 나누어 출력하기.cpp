#include<iostream>

using namespace std;

int main(void) {
	std::ios::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);

	char a[21];

	cin >> a;

	for (int i = 0; i <= a[i] != '\0'; i++) {
		cout << "'" << a[i] << "'" << "\n";
	}



	return 0;
}